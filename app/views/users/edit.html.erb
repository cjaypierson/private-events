<% provide(:title, "Edit User") %>
<h1>Edit User</h1>

<div class="row">
	<div class="col-sm-offset-3 col-sm-6">
		<%= form_for(@user) do |f| %>
			<%= render 'form', f: f %>
			<%= f.submit class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>